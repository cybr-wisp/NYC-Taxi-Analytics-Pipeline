name: "nyc_taxi_analytics"
version: "1.0.0"
config-version: 2
profile: "nyc_taxi_analytics"
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
models:
  nyc_taxi_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
